// This is a placeholder for module enable/disable state.
// In production, fetch this from the backend for each guild/module.
export const defaultModuleStates = {
  commands: true,
  welcome: true,
  embeds: true,
  automod: true,
  'quick-actions': true,
  'role-permissions': true,
  backups: true,
};
